// Environment Configuration for Web (Static HTML)
// This file should be generated at build/deploy time
// NEVER commit with real values!

window.ENV = {
    SUPABASE_URL: 'YOUR_SUPABASE_URL',  // Replace at deploy time
    SUPABASE_ANON_KEY: 'YOUR_ANON_KEY'  // Replace at deploy time
};

// Example of how to use in production:
// 1. Create a build script that replaces these values
// 2. Or serve this file dynamically from your backend
// 3. Or use a static site generator with environment variables
